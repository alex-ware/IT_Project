<div class="page-heading">
    <h1>{{title}}</h1>
</div>

<div class="item-display">
    <div class="left-col">
        <div class="image-box">
            <img src="{{data.image}}" >
        </div>
    </div>
    <div class="right-col">
        <h1>{{data.title}}</h1>
        <h2>{{data.price}}</h2>
        <h4 class="prev-price">{{data.normal_price}}</h3>
        <h4>Cost Reduced {{data.savings}} </h3>
        <br>
        <h4 class="rating-text">{{data.rating}}</h3>
        <p>{{data.review_count}}</p>
        <h3 class="available-text">{{data.availability}}</h3>
        <button class="loginButton">
            <a href="{{data.source}}" target="_blank" >Buy Now</a>
        </button>
        <br>
        <br>
        <input text-align="center" class="loginButton" type="submit" value="Add to Wishlist">
    </div>
</div>
<div class="item-description">
    <hr>
    <div>
        <h2>Description</h2>
    </div>
    <div>
        <p>{{data.description}}</p>
    </div>
</div>
